# 蓝桥杯

## DAY1-模拟基础

### 时间排序

```c++
for(int year=2000;year<=2024;year++)
for(int month=1;month <=12;month++)
for(int day=1;day<=31;day++)
{
    if(month==1||month==3||month==5||month==7||month==8||month==10||month==12);
    else if(month==2)
    {
        if((year%4==0&&year%100!=0)||year % 400 == 0)
        {
            if (day >29) break;
        }
        else
        {
            if(day > 28) break;
        }
        
    }
    else
    {
        if(day > 30) break;
            
    }
}
```

思路：

1. 枚举
2. MOD周期
3. 数字转字符串

### STL

#### map及函数

map函数第一个关键字key，第二个关键字value c

在 C++ 中，`<map>` 是标准模板库（STL）的一部分，它提供了一种关联容器，用于存储键值对（key-value pairs）。

`map` 容器中的元素是按照键的顺序自动排序的，这使得它非常适合需要快速查找和有序数据的场景。

### 定义和特性

- **键值对**：`map` 存储的是键值对，其中每个键都是唯一的。
- **排序**：`map` 中的元素按照键的顺序自动排序，通常是升序。
- **唯一性**：每个键在 `map` 中只能出现一次。
- **双向迭代器**：`map` 提供了双向迭代器，可以向前和向后遍历元素。

### 基本语法

包含头文件:

```
#include <map>
```

声明 map 容器:

```
std::map<key_type, value_type> myMap;
```

- `key_type` 是键的类型。
- `value_type` 是值的类型。

插入元素:

```
myMap[key] = value;
```

访问元素:

```
value = myMap[key];
```

遍历 map:

```
for (std::map<key_type, value_type>::iterator it = myMap.begin(); it != myMap.end(); ++it) {
    std::cout << it->first << " => " << it->second << std::endl;
}
```

### 进阶用法

检查键是否存在:

```
if (myMap.find(key) != myMap.end()) {
    // 键存在
}
```

删除元素:

```
myMap.erase(key);
```

清空 map:

```
myMap.clear();
```

获取 map 的大小:

```
size_t size = myMap.size();
```

#### set及函数

### 语法

C++ 标准库中的 `<set>` 是一个关联容器，它存储了一组唯一的元素，并按照一定的顺序进行排序。

`<set>` 提供了高效的元素查找、插入和删除操作。它是基于红黑树实现的，因此具有对数时间复杂度的查找、插入和删除性能。

`<set>` 容器中存储的元素类型必须满足以下条件：

- 元素类型必须可以比较大小。
- 元素类型必须可以被复制和赋值。

包含头文件:

```
#include <set>
```

声明 set 容器

```
std::set<元素类型> 容器名;
```

### 常用操作

- `insert(元素)`: 插入一个元素。
- `erase(元素)`: 删除一个元素。
- `find(元素)`: 查找一个元素。
- `size()`: 返回容器中元素的数量。
- `empty()`: 检查容器是否为空。

#### 优先队列

## DAY2-暴力枚举

### for和while循环枚举

主要是利用循环嵌套和dfs去求解，但是由于循环层数或递归层数，过多超出时间复杂度的情况，所以用来骗取部分分

#### 蛇形填数：

1. 位置（x，y）
2. 方向，走的方向
3. 终点

写位置指针



一秒大概1e9次的普速运算

#### 数位分解

三重循环

### 利用规律减少枚举次数

#### 成绩数量

long long <2^63

1. 三重循环

时间复杂度过高

2. 优化循环次数

首先第一层循环没有必要从1-n,因为a<=b<=c,如果a*a\*a>n,第一层循环就没必要进行了

对于第二层循环同样因为a<=b<=c,如果a*b\*b>n.第二层循环也没必要进行了

最后我们可以优化掉最内层的循环，最后c的范围b<=c<=n/(a*b),c的数量等于n/(a\*b)-b+1

### 递归

可以认为是数学中的嵌套函数

```c
int func(传入数值){
	if(终止条件) return 最小子问题解	
    return fun(缩小规模)
}
```

#### 递归的特点

递归有两个显著特征，终止条件和自身调用

自身调用：原问题可以分解为子问题，子问题和原问题的求解方法是一致的，即都是调用自身的同一个函数

终止条件：递归必须由一个终止条件，既不能无限循环地调用自身

### 外星 密码



### DFS

树是图的一种特例（联通无环的图就是树）接下来我们看看树用深度优先遍历该怎么遍历

![image-20250331213043924](images/image-20250331213043924.png)

DFS序：遍历树的次序

由于DFS不到返回条件不反回的特性，所以有时候DFS并不是遍历到一个结果就是最有的

```c
全局状态变量

void dfs(当前状态)
{
	if(当前状态是目标状态)
		进行相应处理(输出当前解、更新最优解、退出返回)
	//拓展
        for(所有可行的新状态)
    {
        if(新状态没有访问过&&需要访问)//可行性剪枝、最优性剪枝、重复性剪枝
        {
            标记
            dfs(新状态);
            取消标记
        }
    }
}

```

### 组合枚举

### BFS

横向遍历

BFS按层级逐步探索图，当目标节点在其实节点的相邻节点是，BFS将最快找到该目标节点。这是因为BFS会首先探索所有距离起始节点为的节点，然后是举例为2的节点因此，此目标节点位于其实节点最近的层级是，BFS将会首先到达该目标节点。因此可以仍为BFS最先到达的节点是当前BFS条件下的最优解

```
全局状态变量

void BFS()
{
	定义状态队列
	初始状态入队
	while(队列不为空)
	{
		取出队首状态作为当前状态
		if(当前状态是目标状态)
			进行相应处理(输出当前解，变更最优解、退出返回)
		else：
			for(所有可行的新状态)
			{
			if(新状态没有访问过 && 需要访问)
			{
				新状态入队
			}
			}
			}
			
}
```

![image-20250331220935357](images/image-20250331220935357.png)

![image-20250331221003701](images/image-20250331221003701.png)

## DAY3-基础算法

### 二分查找

```c++
bool check (int x)
{
	//进行某些操作
}

//二分查找函数
int binarySearch()
{
	int l = 1, r = n;
	while( r - l > 1)
	{
		int mid =(l + r) >> 1;
		if (check(mid))
			r=mid;
		else 
			l = mid;
	}
	if (check(l)) //如果满足条件  
		return l;
	else if (check(r))
		return r;
	return -1
}
```

### 快慢指针

求最大不重复区间

### 前缀和

### 差分

![image-20250407202628549](images/image-20250407202628549.png)

### 快速排序

## DAY4-数学基础

## DAY5-数据结构

### 队列

先进先出，后进后出

分单向与双向队列

### 栈

先进后出

### 列表

#### STL List

在c++中，可以使用std::list之来创建一个双向链表的数据结构

1. 包含头文件

   ```c++
   #include <list>
   ```

2. 

### 并查集

### 二叉堆



### 
